global:
  scrape_interval: 15s

scrape_configs:
  # 추후 사용 가능
  # - job_name: 'otel-collector'
  #   static_configs:
  #     - targets: ['otel-collector:8889']

  - job_name: 'spring-boot'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080'] # 서버 주소
